{
  "name": "biblioteca-lbt",
  "private": true,
  "version": "0.4.3",
  "description": "Biblioteca de estudio local-first para LBT",
  "scripts": {
    "start": "http-server . -p 4173 -c-1",
    "test": "node tests/correlations.test.mjs && node tests/static.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
