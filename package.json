{
  "name": "biblioteca-lbt",
  "private": true,
  "version": "0.4.5",
  "description": "Biblioteca de estudio local-first para LBT",
  "scripts": {
    "start": "http-server . -p 4173 -c-1",
    "test": "node tests/correlations.test.mjs && node tests/static.test.mjs && node tests/sync.test.cjs && playwright test",
    "build": "node scripts/build.mjs",
    "check": "npm test && npm run build"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=20"
  }
}
