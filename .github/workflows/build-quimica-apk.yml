name: Build Quimica General APK

on:
  push:
    paths:
      - 'android-chemistry/**'
      - 'content/subjects/quimica_general/units/primer-tramo/**'
      - '.github/workflows/build-quimica-apk.yml'
  pull_request:
    paths:
      - 'android-chemistry/**'
      - 'content/subjects/quimica_general/units/primer-tramo/**'
      - '.github/workflows/build-quimica-apk.yml'
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Install Android SDK packages
        run: sdkmanager 'platforms;android-35' 'build-tools;35.0.0'

      - name: Build installable debug APK
        working-directory: android-chemistry
        run: gradle --no-daemon :app:assembleDebug

      - name: Rename APK
        run: cp android-chemistry/app/build/outputs/apk/debug/app-debug.apk SOLved-Quimica-General.apk

      - uses: actions/upload-artifact@v4
        with:
          name: SOLved-Quimica-General-APK
          path: SOLved-Quimica-General.apk
          if-no-files-found: error
