import fs from "node:fs";import path from "node:path";import {root,unitDirs,read,validateUnit} from "./lib.mjs";const packages=[];for(const dir of unitDirs()){const errors=validateUnit(dir);if(errors.length){console.error(errors.join("\n"));process.exit(1)}const p=read(path.join(dir,"package.json"));packages.push({subjectId:p.subjectId,unitId:p.unitId,title:p.title,contentVersion:p.contentVersion,status:p.status,path:path.relative(root,dir).replaceAll("\\","/")+"/"})}packages.sort((a,b)=>`${a.subjectId}/${a.unitId}`.localeCompare(`${b.subjectId}/${b.unitId}`));const out={catalogSchemaVersion:1,generatedFrom:"package manifests; do not edit manually",packages};fs.mkdirSync(path.join(root,"content"),{recursive:true});fs.writeFileSync(path.join(root,"content/catalog.json"),JSON.stringify(out,null,2)+"\n");console.log(`Catálogo generado: ${packages.length} paquete(s).`);
