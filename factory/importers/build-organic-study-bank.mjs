import fs from "node:fs";
import path from "node:path";
import {load} from "cheerio";

const [primary,corrections]=process.argv.slice(2);if(!primary||!corrections)throw Error("Indicá los dos archivos de consigna");
const root=path.resolve(import.meta.dirname,"../.."),out=path.join(root,"content/subjects/quimica_organica/units/resumen-integral/organic-cards-v2.json"),summaryFile=path.join(root,"content/subjects/quimica_organica/units/resumen-integral/original.html");
const clean=value=>String(value||"").replace(/\s+/g," ").trim(),norm=value=>clean(value).normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g," ").trim(),slug=value=>norm(value).replace(/ /g,"-").slice(0,70);
const unitMap={"ALCANOS":"alcanos","HALUROS DE ALQUILO":"haluros-de-alquilo","ALQUENOS":"alquenos","ALQUINOS":"alquinos","ALCOHOLES":"alcoholes","ÉTERES":"eteres","EPÓXIDOS":"epoxidos","ALDEHÍDOS Y CETONAS":"aldehidos-y-cetonas","ÁCIDOS CARBOXÍLICOS Y DERIVADOS":"acidos-carboxilicos-y-derivados","AROMÁTICOS":"aromaticos","COMPUESTOS NITROGENADOS":"compuestos-nitrogenados","AMINOÁCIDOS Y PROTEÍNAS":"aminoacidos-y-proteinas","HIDRATOS DE CARBONO":"hidratos-de-carbono","ESPECTROSCOPÍA":"espectroscopia"};
const units=Object.entries(unitMap).map(([label,id],index)=>({id,label,index:index+1}));
const $=load(fs.readFileSync(summaryFile,"utf8"),{decodeEntities:true}),anchors=[];$('[id]').each((_,node)=>anchors.push({id:$(node).attr('id'),text:norm($(node).is('img')?$(node).attr('alt'):$(node).find('h1,h2,h3,h4,summary').first().text()||$(node).text().slice(0,180))}));const existingIds=new Set(anchors.map(item=>item.id));$('a[href^="#"]').each((_,node)=>{const id=($(node).attr('href')||'').slice(1);if(existingIds.has(id))anchors.push({id,text:norm($(node).text())})});
function target(title,unit){const words=new Set(norm(title).split(' ').filter(word=>word.length>3)),ranked=anchors.map(item=>({item,score:[...words].filter(word=>item.text.includes(word)).length+(item.text===norm(title)?10:0)})).sort((a,b)=>b.score-a.score);return ranked[0]?.score?ranked[0].item.id:(unit==='espectroscopia'?'atlas-espectroscopia':'indice-maestro-reacciones')}
const category=(mode,id,text,unit)=>mode==='synthesis'?'synthesis':unit==='espectroscopia'?'spectroscopy':/-T\d+/i.test(id)?'theory':/mecanismo|carbocati|radical|enolato/i.test(text)?'mechanism':'reaction';
const tags=title=>norm(title).split(' ').filter(word=>word.length>2).slice(0,8);
const cards=[];let unit="alcanos",unitLabel="Alcanos",topic="Síntesis y reacciones",mode="memory",inSynthesis=false;
for(const raw of fs.readFileSync(primary,"utf8").split(/\r?\n/)){const line=clean(raw);if(/^# 10\. Banco/.test(line)){inSynthesis=true;mode="synthesis";continue}const unitMatch=/^# UNIDAD \d+ — (.+)$/i.exec(line);if(unitMatch){unitLabel=clean(unitMatch[1]);unit=unitMap[unitLabel.toUpperCase()]||slug(unitLabel);topic="General";mode="memory";continue}const synthesisUnit=/^## (.+)$/.exec(line);if(synthesisUnit){topic=clean(synthesisUnit[1]);if(inSynthesis){unitLabel=topic.replace(/ y .*$/i,"");unit=unitMap[unitLabel.toUpperCase()]||slug(unitLabel)}continue}const match=/^\* \*\*(ORG-[A-Z0-9-]+)(?: — ([^*:]+))?:\*\* Frente: (.+?) Dorso: (.+)$/.exec(line);if(!match)continue;const [,id,rawTitle,front,back]=match,title=clean(rawTitle||topic),cardMode=/^ORG-SIN-/i.test(id)?"synthesis":mode;cards.push({id:id.toLowerCase(),unit,unitLabel,topic,subtopic:title,mode:cardMode,category:category(cardMode,id,front+back,unit),front,back,tags:tags(title),summaryTarget:target(title,unit)})}
let correctionGroup="ampliaciones";for(const raw of fs.readFileSync(corrections,"utf8").split(/\r?\n/)){const line=clean(raw),heading=/^### [A-K]\. (.+)$/.exec(line);if(heading){correctionGroup=heading[1];continue}const match=/^\* Frente: (.+?) Dorso: (.+)$/.exec(line);if(!match)continue;const id=`org-add-${slug(correctionGroup)}-${String(cards.filter(card=>card.id.startsWith(`org-add-${slug(correctionGroup)}`)).length+1).padStart(2,"0")}`,unit=/conform/i.test(correctionGroup)?"alcanos":/sn1|ácidos|bases/i.test(correctionGroup)?"haluros-de-alquilo":/adiciones|reorden/i.test(correctionGroup)?"alquenos":/claisen|enolato/i.test(correctionGroup)?"acidos-carboxilicos-y-derivados":/espectro/i.test(correctionGroup)?"espectroscopia":"aromaticos",unitLabel=units.find(item=>item.id===unit)?.label||"Aromáticos";cards.push({id,unit,unitLabel,topic:correctionGroup,subtopic:correctionGroup,mode:"memory",category:category("memory",id,match[1]+match[2],unit),front:match[1],back:match[2],tags:tags(correctionGroup),summaryTarget:target(correctionGroup,unit)})}
const add=(unit,topic,mode,items)=>items.forEach(([front,back,label=topic],index)=>{const id=`org-extra-${unit}-${slug(topic)}-${String(index+1).padStart(2,"0")}`;cards.push({id,unit,unitLabel:units.find(item=>item.id===unit)?.label||unit,topic,subtopic:label,mode,category:category(mode,id,front+back,unit),front,back,tags:tags(`${topic} ${label}`),summaryTarget:target(label,unit)})});
add("aromaticos","Estrategias de síntesis aromática","synthesis",[
["Convertí benceno en m-bromoácido benzoico indicando el orden de sustitución.","1) Acilación o alquilación que permita llegar a COOH. 2) Oxidación de la cadena lateral a COOH. 3) Bromación; COOH dirige meta."],["Diseñá una ruta de benceno a p-bromoanilina evitando la polisustitución del amino.","1) Nitración. 2) Reducción a anilina. 3) Protección como acetanilida. 4) Bromación para mayor proporción para. 5) Hidrólisis de la amida."],["¿Cómo obtendrías m-nitroacetofenona desde benceno?","1) Acilación de Friedel–Crafts con CH₃COCl/AlCl₃. 2) Nitración; el acilo dirige meta."],["Se necesita propilbenceno sin reordenamiento. Proponé una ruta desde benceno.","1) Acilación con CH₃CH₂COCl/AlCl₃. 2) Reducción del carbonilo a metileno, por Clemmensen o Wolff–Kishner."],["Convertí benceno en p-etilnitrobenceno controlando la orientación.","1) Introducir etilo por acilación y reducción. 2) Nitrar; el etilo dirige orto/para y predomina para por menor impedimento."],["¿Cómo convertirías anilina en p-yodoanilina minimizando la polisustitución?","Proteger como acetanilida, yodar en condiciones electrofílicas y luego hidrolizar la protección."],["Diseñá una ruta de tolueno a ácido 3-nitrobenzoico.","Oxidar primero CH₃ a COOH y luego nitrar; COOH dirige meta."],["¿Por qué no conviene intentar una alquilación de Friedel–Crafts directa sobre nitrobenceno? Proponé una alternativa conceptual.","El anillo está fuertemente desactivado y el nitro coordina con el ácido de Lewis. Hay que cambiar el orden: introducir el grupo carbonado antes de nitrar."],["Convertí benceno en p-aminoácido benzoico indicando una estrategia de orden y protección.","Introducir primero una cadena oxidizable, oxidarla a COOH, nitrar según la orientación buscada y reducir NO₂ a NH₂; puede requerirse una estrategia de bloqueo para el isómero para."],["Desde benceno, obtené un haluro aromático pasando por una amina y sin usar halogenación electrofílica directa.","Nitración, reducción a anilina, diazotación con NaNO₂/HCl a 0–5 °C y sustitución de la sal de diazonio con el reactivo apropiado."]]);
add("compuestos-nitrogenados","Sales de diazonio","memory",[["¿Cómo reemplazás NH₂ de una arilamina por Cl?","Formar primero ArN₂⁺ con NaNO₂/HCl a 0–5 °C; luego usar CuCl (Sandmeyer).","cloruro aromático"],["¿Cómo reemplazás NH₂ de una arilamina por Br?","Diazotar a 0–5 °C y tratar con CuBr.","bromuro aromático"],["¿Cómo reemplazás NH₂ de una arilamina por I?","Diazotar a 0–5 °C y tratar la sal con KI.","yoduro aromático"],["¿Cómo reemplazás NH₂ de una arilamina por F?","Diazotar, formar el tetrafluoroborato con HBF₄ y calentarlo (Balz–Schiemann).","fluoruro aromático"],["¿Cómo reemplazás NH₂ de una arilamina por CN?","Diazotar a 0–5 °C y tratar con CuCN.","nitrilo aromático"],["¿Cómo convertís una arilamina en fenol?","Diazotar con NaNO₂/HCl a 0–5 °C y calentar la sal acuosa para hidrolizarla.","fenol"],["¿Cómo eliminás el grupo amino de una arilamina y lo reemplazás por H?","Diazotar y reducir la sal de diazonio con H₃PO₂.","desaminación"],["¿Cómo preparás un colorante azoico desde una arilamina?","Diazotar a 0–5 °C y acoplar la sal con un anillo aromático activado, como fenolato o una anilina, controlando el pH.","acoplamiento azoico"]]);
add("espectroscopia","Problemas integradores","memory",Array.from({length:20},(_,i)=>{const cases=[
["Una fórmula C₃H₆O muestra IR a 1715 cm⁻¹ y RMN con un único singlete de 6 H. Proponé una estructura.","Propanona. El IR indica carbonilo y la equivalencia de los dos metilos explica el singlete de 6 H."],["C₂H₆O no presenta banda O–H y su RMN muestra triplete de 3 H y cuarteto de 2 H. ¿Qué estructura es compatible?","Dimetil éter no coincide con ese patrón; los datos son internamente incompatibles para C₂H₆O. No hay información coherente para una asignación única."],["Un compuesto C₄H₈O₂ tiene IR intenso cerca de 1740 cm⁻¹ y RMN con triplete 3 H, cuarteto 2 H y singlete 3 H. Proponé una estructura.","Acetato de etilo: el éster explica el IR, el etilo da triplete/cuarteto y CH₃CO da singlete."],["Una propuesta contiene un alcohol, pero el IR carece de banda ancha entre 3200–3600 cm⁻¹. ¿Qué conclusión sacás?","La ausencia de O–H contradice la estructura alcohólica, salvo condiciones experimentales excepcionales; debe descartarse o revisarse la muestra."],["C₄H₈O da IR de carbonilo y RMN con señales que integran 3:2:3. ¿Alcanza para distinguir aldehído de cetona?","No por sí solo. La ausencia o presencia de un protón aldehídico cerca de 9–10 ppm es necesaria para decidir."],["El ion molecular es 86, el IR muestra C=O y la RMN tiene doblete de 6 H y septeto de 1 H. ¿Qué fragmento sugiere la RMN?","Un grupo isopropilo. Combinado con el carbonilo y la masa, orienta hacia una cetona compatible, pero se debe verificar la fórmula molecular."],["Una molécula tiene índice de insaturación 4, UV-Visible de conjugación aromática y RMN con cinco protones aromáticos. ¿Qué patrón estructural sugiere?","Un anillo bencénico monosustituido. Falta identificar el sustituyente con IR, masa y señales restantes."],["C₃H₆O₂ presenta IR muy ancho 2500–3300 cm⁻¹ y C=O cerca de 1710 cm⁻¹. ¿Qué grupo funcional domina?","Ácido carboxílico; la banda O–H muy ancha junto con el carbonilo es diagnóstica."],["Una estructura propuesta exige dos grupos metilo equivalentes, pero la RMN muestra dos singletes de 3 H diferentes. ¿Qué indica?","Los metilos no son equivalentes en el entorno real; la estructura propuesta o su simetría es incorrecta."],["Un compuesto con fórmula C₄H₈ tiene índice de insaturación 1, pero no decolora Br₂ y no muestra señales vinílicas. ¿Qué alternativa queda?","Un cicloalcano es más compatible que un alqueno." ]];return cases[i%cases.length]}));
const ids=new Set();for(const card of cards){if(ids.has(card.id))throw Error(`ID duplicado: ${card.id}`);ids.add(card.id);if(!card.unit||!card.topic||!card.front||!card.back)throw Error(`Tarjeta incompleta: ${card.id}`);if(!anchors.some(anchor=>anchor.id===card.summaryTarget))throw Error(`Ancla inexistente: ${card.id} -> ${card.summaryTarget}`)}
const topics=[...new Set(cards.map(card=>`${card.unit}|${card.topic}`))].map(value=>{const [unit,id]=value.split('|');return{id:slug(`${unit}-${id}`),unit,label:id}}),payload={schemaVersion:2,contentVersion:"2.0.0",generatedAt:"2026-08-03T00:00:00.000Z",units,topics,cards};fs.writeFileSync(out,`${JSON.stringify(payload,null,2)}\n`);console.log(JSON.stringify({cards:cards.length,memory:cards.filter(card=>card.mode==='memory').length,synthesis:cards.filter(card=>card.mode==='synthesis').length,topics:topics.length,subtopics:new Set(cards.map(card=>`${card.unit}|${card.topic}|${card.subtopic}`)).size}));
